name: C003 Authorized One-Time Render
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/c003-authorized-once.yml'
permissions:
  contents: read
concurrency:
  group: zaskaleta-master-clone-gpu
  cancel-in-progress: false
jobs:
  diagnostic:
    if: github.event_name == 'push' && github.event.head_commit.message == 'Diagnose failed C003 kernel without retry'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
      KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
    steps:
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Fetch exact failed C003 logs
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --quiet 'kaggle>=1.8.0'
          HANDLE="$KAGGLE_USERNAME/zaskaleta-c003-recovery-34074157342-1"
          kaggle kernels status "$HANDLE" || true
          kaggle kernels logs "$HANDLE" || true
          echo 'Diagnostic only; no kernel push or GPU retry.'
  render-and-export:
    if: github.event_name == 'push' && github.event.head_commit.message == 'Retry C003 preserving approved reference FPS after double preflight'
    runs-on: ubuntu-latest
    timeout-minutes: 135
    env:
      KAGGLE_API_TOKEN: ${{ secrets.KAGGLE_API_TOKEN }}
      KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
      KAGGLE_PRIVATE_DATASET: ${{ secrets.KAGGLE_PRIVATE_DATASET }}
      PYTHONUNBUFFERED: '1'
      C003_SOURCE_SHA: 77366de171a39fecfe85aa5c8b348d137a10c507
    steps:
      - uses: actions/checkout@v4
        with:
          ref: 77366de171a39fecfe85aa5c8b348d137a10c507
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Verify pinned source and Kaggle authentication
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$C003_SOURCE_SHA"
          test -n "$KAGGLE_API_TOKEN"
          test -n "$KAGGLE_USERNAME"
          test -n "$KAGGLE_PRIVATE_DATASET"
          python -m pip install --quiet 'PyYAML>=6,<7' 'kaggle>=1.8.0' 'kagglehub>=0.3.13'
          kaggle kernels list --mine --page-size 1 >/dev/null
          kaggle datasets files "$KAGGLE_PRIVATE_DATASET" >/dev/null
      - name: Double complete CPU preflight
        shell: bash
        run: |
          set -euo pipefail
          export GITHUB_SHA="$C003_SOURCE_SHA"
          for pass in 1 2; do
            echo "C003_APPROVED_PREFLIGHT_PASS_${pass}"
            python kaggle/test_c003_release.py
            python kaggle/c003_release_safety.py --verify
            python kaggle/audit_c003_source.py
            python kaggle/recover_c003_readonly.py --dry-run
          done
          test "$(git rev-parse HEAD)" = "$C003_SOURCE_SHA"
      - name: Submit exact C003 and retrieve verified output
        shell: bash
        run: |
          set -euo pipefail
          export GITHUB_SHA="$C003_SOURCE_SHA"
          python kaggle/recover_c003_readonly.py
      - name: Upload exact safe review files
        uses: actions/upload-artifact@v4
        with:
          name: master-clone-c003-verified-${{ github.run_id }}
          path: |
            .kaggle-review/MASTER_CLONE_GATE_08_15_CANDIDATE_003.mp4
            .kaggle-review/MASTER_CLONE_GATE_08_15_CANDIDATE_003.provenance.json
            .kaggle-review/C003_verified_manifest.json
          if-no-files-found: error
          retention-days: 7
      - name: Safety summary
        if: always()
        run: |
          echo 'User-authorized free Kaggle C003 attempt. No paid GPU, stable overwrite, or promotion.'
          echo 'Only the verified MP4 and sanitized review metadata may be uploaded.'
