name: Kaggle MASTER CLONE Autopilot (legacy frozen)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-frozen:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Legacy C002 rendering is frozen during C003 preflight.'
          echo 'Use the audited, explicitly selected C003 workflow only.'
          echo 'No GPU submission, stable overwrite, or automatic promotion.'
