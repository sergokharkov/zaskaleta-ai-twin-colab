name: Kaggle MASTER CLONE C003 Isolated (legacy frozen)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-frozen:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Superseded isolated C003 workflow is frozen.'
          echo 'No GPU submission or old-kernel follow is permitted.'
          echo 'Use the audited C003 Readonly Recovery after release approval.'
