name: Kaggle C003 Recovery (legacy frozen)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-frozen:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Superseded C003 recovery is frozen.'
          echo 'Read-only filesystem failure is fixed only in the newer recovery.'
          echo 'No GPU submission is permitted through this workflow.'
