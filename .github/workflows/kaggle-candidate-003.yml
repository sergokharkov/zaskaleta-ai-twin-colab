name: Kaggle MASTER CLONE Candidate 003 (legacy frozen)
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  legacy-frozen:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Shared-kernel C003 rendering is frozen.'
          echo 'This workflow cannot submit a GPU job or follow stale C002 output.'
          echo 'Use the audited, explicitly selected C003 recovery only.'
